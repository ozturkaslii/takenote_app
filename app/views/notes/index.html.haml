.wrapper_with_padding
	- unless @notes.blank?
		- @notes.each do |note|
			%a{ href: (url_for [note])}
				.note
					%p.title= note.title
					%p.date= time_ago_in_words(note.created_at)
	- else
		%h2 Add a note
		%p Don't you wanna create a note?!
		%button= link_to "New Note", new_note_path
